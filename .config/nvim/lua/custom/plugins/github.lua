return {
  {
    'github/copilot.vim',
    config = function()
      -- Optional: set some Copilot options
      -- vim.g.copilot_no_tab_map = true -- disable default <Tab> mapping
      -- disable tab in edit mode and use it for copilot:
    end,
  },
}
